name: "Scenario 5: Legitimate update (tag moved forward)"

# Pinned at 38b3412 before the maintainer bumped @updated to ea53476
# (a descendant). This is the normal case — not an attack.
#
# Expected: gh actions-pin check → ref_moved (reachability passes)
# Review the diff, then `gh actions-pin upgrade` to accept.

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: nodeselector/actions-test-fixtures/nested-composite@updated



# Automatically generated and managed by: `gh actions pin <workflow-path>`
dependencies:
  - github.com/actions/checkout@v4:sha1-34e114876b0b11c390a56381ad16ebd13914f8d5
  - github.com/nodeselector/actions-test-fixtures/nested-composite@updated:sha1-38b3412adcb7afb4a061c519513e45cbaf4a1cec
